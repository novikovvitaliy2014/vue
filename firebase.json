{
  "functions": {
    // "predeploy": [
    //   "npm --prefix \"$RESOURCE_DIR\" run lint"
    // ]
    "predeploy": [
      "npm --prefix \"%RESOURCE_DIR%\" run lint"
      ]
  }
}

{
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
